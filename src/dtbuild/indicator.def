R"(
%INCLUDES%

struct %INDICATOR_CLASS%
{
	double ratio = 0.0;
	%INDICATOR_CLASS%(double _ratio)
	{
		ratio = _ratio;
	}

	std::string getName()
	{
		return "%INDICATOR_LABEL%";
	}

	indicator_data calculate(const candleset& candles)
	{
		unsigned int length = candles.size();
		unsigned int range = length * ratio;
		std::vector<double> data(length, 0.0);
		
		%CALCULATE_SCRIPT%
		
		return { getName(), data };
	}
};
)"
