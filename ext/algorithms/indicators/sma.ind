$label "SMA"
$identifier SMA


double sum = 0.0;
unsigned int dataIndex = 0;
for(unsigned int i = 0; i < range; i++)
{
	sum += candles[i].close;
	data[i] = sum / (double)(i + 1);
}

for(unsigned int i = range; i < length; i++)
{
	sum = 0.0;
	unsigned int si = (i + 1) - range;
	int count  = 0;
	for(unsigned int j = si; j <= i; j++)
	{
		sum += candles[j].close;
		count++;
	}

	data[i] = sum / (double)range;
}
