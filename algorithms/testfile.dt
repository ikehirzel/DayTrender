indicator EMA(double ratio)
{
	for (int i = 0; i < 3; i++)
	{
		print("HELLO\n");
	}
	if (s == 'hello')
	{
		return 0;
	}
}

algorithm Simple_MA(EMA<0.5> short, EMA<1.0> long)
{
	print(short[0]);
	this.buy();
}
